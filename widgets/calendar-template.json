{
  "type": "AdaptiveCard",
  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
  "version": "1.5",
  "body": [
    { "type": "TextBlock", "text": "📅 Upcoming Events", "weight": "Bolder" },
    {
      "$data": "${events}",
      "type": "Container",
      "items": [
        {
          "type": "TextBlock",
          "text": "${time} – ${title}",
          "wrap": true
        }
      ]
    }
  ]
}
